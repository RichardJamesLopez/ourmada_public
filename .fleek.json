{
    "build": {
      "image": "node:alpine",
      "command": "npm install && npm run build && echo $SOME_ENV",
      "publicDir": "build",
      "baseDir": "main",
      "environment": {
        "SOME_ENV": "Build finished!"
      }
    }
   }
